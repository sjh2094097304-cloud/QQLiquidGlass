# 构建与检查记录（2026-09-02）

- 版本：0.4.2-qqdock.6 / versionCode 10
- 包名：io.github.liuran001.mmliquidglass
- 产物：LiquidGlass-v0.4.2-qqdock.6.apk
- APK SHA-256：747175ca3d3056322e5b5b4b975611ab1bd841e03ec5c1b6887b62a5213b41d0
- 上游基线：003d1a8d9c8d80fac4f3a63ef35835dc8d3a4e0e
- APK minSdk 26 / targetSdk 34 / compileSdk 35。QQ 液态底栏和实时预览要求 API 33 以上。
- JDK 17.0.20.1、Android build-tools 35.0.0、libxposed API 102.0.0
- javac、D8、aapt2、zipalign、apksigner：通过；只有 deprecated API 提示。
- 签名证书 SHA-256：8eeb5e14642d67b67139efe5dbacf0ce8550b6346a2d5cc6ca8f41b9edc8d867，与本对话上一版定制包一致。
- 原有几何／标签断言：1518 项通过。
- 配置范围／草稿／几何组合断言：32911 项通过。
- 自定义颜色与预览位置断言：14175 项通过，覆盖 RGB 黑／白、大小写、简写、无效输入、颜色往返、草稿隔离、旧色板保持、预设保留自定义色、悬浮位置单调与边界。
- 工作合并（100000 事件）、点击防重入与取消令牌：通过。
- 日志容量、去重、关闭／清空及脱敏：通过。
- 实际手势控制器的主机端测试：原有点击／拖动／取消／暂停继续通过；新增零按压放大强度和 125% 强度检查通过。
- 实际共享背景类的主机端测试：缓存复用、重入保护、暂停和异常继续通过；新增“预览采样与错误不覆盖真实 QQ 背景诊断”检查通过。
- 源码检查：预览复用生产光效和弹簧；预览不保存草稿、不调用原生点击；关闭时移除更新与动画回调；输入键盘出现时收起预览。原有头像／设置／转场保护检查继续通过。
- 构建脚本语法及 git diff --check：通过。

## 实现范围

自定义色作为 RGB 单独保存，颜色选择模式新增“自定义”，不挤占原来四个颜色值。色相／饱和度／明度和十六进制输入都修改设置草稿，保存后写入已有 QQ 私有配置。未选中标签不透明度、底色随强调色、按压强度默认维持上一版效果。

预览使用独立的模块示例场景，真实执行与首页相同的玻璃、液滴和标签绘制。可操作的预览标签只修改预览选中项；不会转发 QQ 点击或打开侧边栏。头像使用现有模块缓存的独立副本，不新增屏幕采样。预览宽度适配设置面板，短预览区只适配纵向位置，不再整体缩小栏体与文字；放大预览覆盖完整悬浮距离范围。

预览修改按显示帧合并，不常驻刷新；手势结束后弹簧自行停止，隐藏、关闭和窗口不可见时停止动画。反馈页及键盘输入时让出预览空间。

## 验证边界

用户已明确确认 0.4.1 在其设备上正常。本次开发环境没有 Android 设备、模拟器或目标 QQ 运行实例。主机端测试使用明确的 Android 测试替身，仅验证控制代码、状态与调用边界，不是 GPU 或 QQ View 的实际运行测试。

新增颜色编辑器、预览着色器最终画面、示例背景滚动、头像副本显示、键盘和放大布局，仍需在 QQ 9.2.75 (13520) / Android 16 API 36 真机确认。没有宣称已对新 UI 完成实机截图验证。未做微信真机回归。

参见 README-CUSTOM.md 的使用和安装说明。

- qqdock.6：新增 QQ 当前主题底栏图标读取/缓存/预览路径；未识别图标时保持模块线条图标回退。
