# qqdock.6 变更

本版新增「底栏图标跟随 QQ 主题」开关。

- 关闭：继续使用模块自绘线条图标。
- 开启：在「仅图标」或「图标＋文字」模式下，读取 QQ 当前主题/个性装扮的原生底栏图标，频道等全部标签同步跟随。
- 若当前 QQ 版本/主题无法识别图标 View，会自动回退模块线条图标，不影响点击、长按、液滴、未读数字与头像逻辑。
- 主题图标使用小尺寸缓存位图同步，避免悬浮底栏每帧直接重绘 QQ 原生 View；设置页实时预览会读取当前 QQ 图标快照。
- 左侧长按仍完全交给 QQ 原生逻辑。

验证：`tests/run.sh` 全部通过。
