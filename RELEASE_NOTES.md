# 百变气泡 v3.7 更新日志

## v3.7（versionCode 28）

以下为本次源码修复记录；本地逻辑回归已通过，APK 构建与真机验收状态见 `VERIFICATION.md`。

### ID 过滤与菜单

- 气泡 ID 只从当前消息的 `msgAttrs → vasMsgInfo → bubbleInfo` 读取，不再递归扫描正文、引用记录、转发记录或任意对象字段。
- 明确默认主气泡 ID（包括 0、115）不会使用残留子 ID；仅带表情/贴纸而没有真实文字气泡的消息被过滤。
- 新旧菜单都在插入入口前校验，点击时再校验一次。
- 商城只接受完整 `2_` 类型 ID，统一处理 URL/Bundle、编码、数字边界和参数冲突；裸 ID 不再抢占真实气泡 ID。
- 菜单操作按对象身份记录，修复 QQ 菜单重载 `equals/hashCode` 后误接管其他菜单项的问题。
- 自定义菜单视图检查返回类型，头像保持 24dp；保留原图片和资源加载方式。

### 配置与流畅性

- Provider 和 QQ 本地存储共用 `ConfigStore`，避免两套写入、删 ID、开关互斥逻辑漂移。
- 设置先落盘再提示成功；提交失败回滚进程内映射，错误返回不会替换有效缓存。
- 本地修改持久记录待同步状态，Provider 恢复前先回放本地修改；升级时保护 3.6 未记录脏标记的现有配置。
- 配置缓存使用固定的来源刷新期限；发送统计不再续期。发送路径读取内存配置，首次发送直接读取本地状态，不等待启动 IPC 锁；Provider 同步在后台执行。
- Provider 连续失败采用 30/60/120/240 秒退避；发送统计合并写入；相同远端快照避免重复落盘。
- 旧异步读取及旧镜像不能覆盖后来操作或并发本地写入；配置快照复制数组，避免调用方意外修改缓存。
- 删除正在使用的固定 ID，即使池内还有其他 ID，也同步清除固定设置。
- 导入检查 JSON 结构、字段类型、整数范围、嵌套深度和 300 个 ID 上限；纯 ID 导入保留未指定的防撤回设置。
- 反射查询缓存和去重日志设置容量上限；频繁相同错误不会永久延长日志抑制时间。

### 随机气泡

- 使用完整数组比较识别换池，修复不同池哈希碰撞后继续发旧 ID。
- 保持 Fisher–Yates 洗牌、轮内去重和跨轮相邻去重。
- 运行中的上一 ID 不再被过期磁盘统计反复覆盖；发送重载嵌套调用只消耗一次随机选择。

### 防撤回与兼容

- 同步包逐条移除已识别的撤回记录，保留同批正常消息、未知字段及已识别的自身私聊路由。
- 单独撤回推送跳过已确认返回 void 的回调，不再把空 protobuf 传给 QQ。
- 先筛选相关命令，再读取配置与解析；畸形/超大包保留原处理路径并记诊断。
- 补上底部粉色文字，考虑键盘遮挡；后台发生的拦截在返回 QQ 后提示；合并提示避免堆叠。
- 增加 Application 已创建时的延迟注入入口、空 applicationContext 回退、页面暂停后的活动引用清理。
- 自检实际测试本地读写，并分别显示发送/菜单/商城/防撤回 Hook 是否挂载、是否触发。没有检测到 FPA 不再自动宣称 FPA 已激活。
- 不宣称所有框架和 Android 16/17 均已实机通过。

### 升级与交付

- 版本统一为 3.7 / 28，保留原 `.v36` 应用 ID、Provider authority、配置位置、公告状态、作者信息和签名保护。
- 应用与 QQ 内设置共用 3.7 日志；新增离线回归测试、本地原签名构建脚本和 GitHub Actions 工作流。
- 正式 APK 尚未产出：当前环境缺少 Android SDK，下载构建工具所需的网络提权被审批策略拒绝。

---

以下保留原源码的历史说明，不代表本次重新验证了当年的全部兼容声明。

## 历史版本记录

## v3.6 长期运行自愈修复（版本名称与内部版本 27 保持不变）

- 配置缓存由永久缓存改为每 20 秒轻量刷新，适配 QQ/FPA 后台恢复和进程状态变化。
- 发送前按气泡池、独立 ID 与模式开关重新校验有效状态。
- 检测到主开关与随机/独立模式不一致时自动修复并持久保存，不再要求手动重新开关。
- 设置页开关只按真实有效配置显示，随机模式必须有气泡池，独立模式必须有有效 ID。
- 保留上一次有效缓存作为读取失败兜底，避免临时通道异常让气泡回到默认。

## v3.6 最终两项修复（版本名称与内部版本 27 保持不变）

- 修正 Provider 重试结束后直接抛异常、没有进入 QQ 本地兼容通道的逻辑错误。
- 配置实际可读写时自检显示正常，并注明真实配置通道及 Provider 降级原因。
- 商城只接受 `itemid/item_id/kr_turbo_display=2_XXXX` 类型的明确气泡资源。
- 挂件、头像、主题和其他共用商城容器不再触发气泡 ID 弹窗。
- 长按菜单头像固定为 24dp × 24dp，保持 QAuxiliary 风格的小型正方形比例。

## v3.6 三项实机修复（版本名称与内部版本 27 保持不变）

- 修复独立安装包仍使用旧包名定位头像的问题。
- 长按“秋天”菜单增加渲染后头像替换，避免显示 QQ 的滑杆占位图标。
- 商城恢复真正气泡资源的 ID 弹窗，同时过滤挂件、头像与主题资源。
- 消息气泡 ID 增加 4—10 位最终校验，无气泡消息不再把 `115` 误识别为气泡 ID。
- 诊断日志明确记录被阻止的无效消息 ID，并保留真实 Hook 异常位置。

## v3.6 Hook 入口修复版（内部版本 26）

- 保持显示版本 3.6，并将 Android 内部版本提升到 26，避免被识别为低版本。
- 修复 FPA/免 Root 框架提供虚拟模块路径时，启动前签名检查异常导致整条 Hook 链中断。
- 签名路径检测失败时仅记录诊断，不再阻止 QQ 设置入口、长按菜单和发送气泡 Hook。
- Hook 状态同步失败与各项功能继续相互隔离，单项异常不会让“QQ → 设置 → 气泡”消失。
- 保留 3.6 的头像重试、商城模式选择、主题过滤、配置导入、错误代码和提示优化。

## v3.6 Hook 修复版（内部版本 24）

- 修复上一份 v3.6 APK 被 R8 过度收缩，导致 Hook 实现未完整进入 DEX。
- 恢复完整传统 Xposed/FPA 入口、QQ 作用域及全部 Hook 类。
- 关闭资源收缩，避免框架间接访问的资源被误删。
- 内部 versionCode 提升到 24，显示版本保持 3.6，可由系统识别为更新。
- 保持原应用 ID 与原签名证书，可直接覆盖旧版。

## v3.6

- 修复清除应用数据或框架重载后，长按菜单头像不再重新加载的问题。
- 修复 FPA 环境配置读写、自检误报和导入配置失败。
- 商城使用气泡时可选择独立模式或随机模式，不再默认切换到独立模式。
- 商城资源增加气泡类型校验，避免将主题资源误识别为气泡 ID。
- 清空气泡池后自动关闭全部模式，并明确提示需要手动重新开启。
- Bug 反馈改为自动复制作者 QQ 邮箱并打开邮箱应用。
- 诊断日志增加智能错误代码、异常位置、配置通道与 Hook 状态。
- 没有气泡的消息不再显示气泡 ID 获取弹窗。

## v1.3.5

- 优化 QQ 9.3.50 与 FPA 3.6 的配置同步稳定性。
- 修复首次公告状态保存与重复显示问题。
- 修复长按菜单头像加载及部分消息气泡 ID 识别。
- 优化随机气泡算法，全部 ID 每轮随机打乱且不重复。
- 优化防撤回文字提醒与模块状态检测。
- 减少设置扫描与跨进程写入，取消 QQ 全局资源表挂载，降低多模块冲突。
- 升级粉色 Liquid Glass 界面与交互细节。
- 提升 Android 16/17 运行兼容性与模块整体流畅性。

## v1.3.3

- 加入公告记忆、全屏防倒卖水印与签名完整性保护。
- 优化应用头像、长按菜单图标和粉色玻璃质感。

## v1.3.2

- 重构独立模式、随机模式与气泡 ID 管理。
- 设置修改支持即时保存并在 QQ 内生效。

## v1.2.3

- 独立模式与随机模式改为互斥开关。
- 增加独立气泡 ID 管理入口、格式校验与重复拦截。
- 长按消息菜单增加“秋天”入口并优化显示位置。

## v1.2.2

- 每个气泡 ID 改为独立卡片，可复制、固定或移除。
- 新增 ID 改为逐个添加，保存逻辑更加清晰。

## v1.2.1

- 修复 QQ 主题导致模式开关样式异常的问题。
- 增强 QQNT 长按菜单基类与消息类型兼容。

## v1.2.0

- 将气泡功能真正接入 QQ 设置页面。
- 增加设置入口接管、商城 ID 深层识别与长按消息解析。
- 外部应用精简为作者、反馈、公告及模块说明。

## 作者与移植说明

基于疑是天上星的气泡脚本，由秋天移植为模块。作者 QQ：3694476602。
