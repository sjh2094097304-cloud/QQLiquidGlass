# 3.7 修复与验证记录

基于上传的 `气泡3.6源码.zip`，2026-09-01。原文件保持未修改。

## 当前完成情况

| 项目 | 结果 |
| --- | --- |
| 3.7 核心逻辑修复 | 已修改，见 RELEASE_NOTES.md |
| 应用与 QQ 内 3.7 修复日志 | 已增加，共用 ReleaseNotes |
| 逻辑回归 | 13 组通过，具体断言数见 tests/results-3.7.txt |
| 应用全部 22 个 Java 文件 | 语法解析及裁剪 API 接口桩编译通过 |
| 原发布 p12 与源码签名指纹 | 已核对一致；密码和私钥未加入源码 |
| 真实 Android/Gradle/R8/DEX 构建 | 未完成：缺少 Android SDK/构建缓存 |
| 3.7 可安装 APK | 未生成，不能交付或声称已编译 |
| QQ 9.2.75、Android 16/17、FPA/LSPosed 实机 | 当前环境没有设备，未执行 |
| GitHub 发布 / Actions | 无可写仓库授权；只准备工作流，未推送、未运行 |

环境探查确认有 JDK 17，无 Android SDK、D8/R8、aapt/apksigner 及可用 Gradle 分发缓存。
获取官方 Android 构建工具的网络请求未获批准，随后显式提权请求被自动审批策略拒绝，拒绝原因是当前策略不允许 `sandbox_approval`/提权请求。未改变该策略或绕过限制。

最终构建脚本实际运行到原证书核对通过，随后因缺少 Android SDK 返回非零状态。现场输出见 `tests/build-attempt-3.7.txt`。

## 关键缺陷与对应处理

| 原问题 | 3.7 处理 | 验证层级 |
| --- | --- | --- |
| 一个同步撤回导致同批普通消息也被删除 | 按消息逐条过滤，其他内容字节保留 | 真实协议逻辑回归 |
| Provider 恢复覆盖新的本地配置 | 本地先持久化、待同步标记、版本比较、镜像修订检查 | 模拟离线、恢复、并发和失败 |
| 旧版离线修改没有待同步标记 | 首次 3.7 读取保护已有本地配置 | 3.6 数据迁移回归 |
| 错误 Bundle 被当作正常配置 | 检查完整成功返回；保留最后有效缓存 | 存储类型错误回归 |
| 连续发送导致配置始终不刷新 | 来源时间与发送统计时间分离，后台刷新 | 可控时钟回归 |
| 删除当前固定 ID 后独立模式恢复 | 两种存储统一清除固定 ID 和开关 | Provider/本地回归 |
| 非法 JSON、浮点/溢出 ID 被误接受 | 结构、类型、范围、深度与容量校验 | Android JSON 实现执行 |
| 换池哈希碰撞后发出旧 ID | 完整数组比较及洗牌状态重建 | 确定碰撞回归 |
| 失败的磁盘写入也提示成功 | 检查 commit、回滚内存、完成后反馈 | 存储回归；UI 真机待验收 |
| 自检没试写、只凭设置入口判定 Hook 正常 | 实际探针写入，按功能记录挂载/触发 | 存储/状态逻辑回归 |
| 表情、默认气泡、转发内容误弹 ID | 只读当前消息 VAS 属性；插入前过滤 | 消息模型回归；QQ 菜单待验收 |
| 装扮商城共享页面误取 ID | 完整 2_ 标记、边界与参数冲突判定 | URL/Bundle 规则回归 |
| QQ 原菜单因 equals/hashCode 被误接管 | 弱引用身份映射 | 菜单对象回归 |
| 主线程与推送路径重复 IPC/扫描 | 发送走内存、后台同步、退避、缓存查询、合并写入 | 慢 IPC 阻塞回归；帧率待测 |
| 防撤回底部文字看不到 | 装到 decor、考虑可见区域、后台返回补提示 | Java 编译检查；真机待验收 |
| 源码 versionCode 26 低于原 APK 27 | 改为 28，显示 3.7，保留应用身份 | 构建配置检查；安装待验收 |

本次编译的 Android 接口桩是基于 AOSP 公开签名制作的测试辅助文件，含省略/简化，不等同于官方 Android SDK。没有用测试桩制作安装包。

## 已保留的内容

- `.v36` 应用 ID、Provider authority、QQ 本地配置文件名及已确认公告的键名。
- 原默认气泡池、界面配色/布局、作者及移植说明、原签名保护。
- 两份头像图片、NOTICE、xposed_init、xposed_scope 与原源码逐字节一致。
- 原始 3.6 源码和 APK 保持未修改，可作回退和对照。

## 仍需实机确认

- 当前消息属性模型、QQ 菜单视图、商城路由随 QQ 版本变化的兼容性。
- 无 root 框架的不同注入时机、进程隔离与模块更新方式。
- Android 16/17 的键盘、全面屏及窗口层级对底部提示的影响。
- 自身群撤回及其他不能仅靠路由字段判断操作者的协议变体。
- 防撤回提示是短时底部文字，不会永久插入聊天灰条，也不会恢复安装前已删除的消息。
- 滚动/发消息帧率、内存和耗电：没有真实设备基线，不给出未经测量的性能提升百分比。

有完整 SDK 环境后，运行 BUILDING.md 中的原签名构建命令，再按其中验收顺序测试。未知宿主结构会跳过未匹配功能并记录日志，不能把“自检能打开”等同于全功能稳定。
